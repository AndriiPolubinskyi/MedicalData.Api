@inject NavigationManager Nav

<p style="color:orange;font-size:18px;">NOT-AUTH — redirecting…</p>

@code {
    protected override void OnInitialized()
    {
        Nav.NavigateTo("/login", forceLoad: false);
    }
}
